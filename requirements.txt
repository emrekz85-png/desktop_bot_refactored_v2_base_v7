# Desktop Bot Trading Application Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
pandas>=1.5.0
pandas_ta>=0.3.14b
numpy>=1.21.0
requests>=2.28.0
python-dateutil>=2.8.0
matplotlib>=3.5.0

# Performance optimization (highly recommended - ~50-100x faster indicator calculations)
numba>=0.56.0

# Optional: Progress bars (recommended for Colab/Jupyter)
tqdm>=4.64.0

# Optional: GUI mode (not needed for headless/Colab)
# Uncomment the following lines if you need GUI support:
# PyQt5>=5.15.0
# PyQtWebEngine>=5.15.0
